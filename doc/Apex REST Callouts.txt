## 🔥 1. Apex Class

```apex
public class AnimalLocator {
    public static String getAnimalNameById(Integer id) {
        Http http = new Http();
        HttpRequest req = new HttpRequest();
        
        req.setEndpoint('https://th-apex-http-callout.herokuapp.com/animals/' + id);
        req.setMethod('GET');
        
        HttpResponse res = http.send(req);
        
        if (res.getStatusCode() == 200) {
            Map<String, Object> result = (Map<String, Object>) JSON.deserializeUntyped(res.getBody());
            return (String) result.get('name');
        }
        return null;
    }
}
```

---

## 🔥 2. Mock Class

```apex
@isTest
global class AnimalLocatorMock implements HttpCalloutMock {
    global HTTPResponse respond(HTTPRequest req) {
        HttpResponse res = new HttpResponse();
        res.setHeader('Content-Type', 'application/json');
        res.setBody('{"id":1,"name":"lion"}');
        res.setStatusCode(200);
        return res;
    }
}
```

---

## 🔥 3. Test Class (100% coverage)

```apex
@isTest
public class AnimalLocatorTest {
    @isTest
    static void testGetAnimalNameById() {
        Test.setMock(HttpCalloutMock.class, new AnimalLocatorMock());
        
        Test.startTest();
        String result = AnimalLocator.getAnimalNameById(1);
        Test.stopTest();
        
        System.assertEquals('lion', result);
    }
}
```

---

## ⚠️ CHECKLIST (rất quan trọng)

* Class name đúng: `AnimalLocator`
* Method đúng: `getAnimalNameById(Integer)`
* Return đúng: `String`
* Endpoint đúng format:

```txt
https://th-apex-http-callout.herokuapp.com/animals/<id>
```

* Có mock → không bị fail test
* Coverage = 100%

---

## 🧠 1 dòng để nhớ

```txt
Callout test = Test.setMock() + fake response
```
